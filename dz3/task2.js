
const prompt = require('prompt-sync');

function findMinimum(...args) {
    return Math.min(...args);
}

function collectNumbers() {
    const numbers = [];
 
    while (true) {
        const input = prompt("введите число или стоп: ");

        if (input == "стоп") {
            brake
        }
    }
}
